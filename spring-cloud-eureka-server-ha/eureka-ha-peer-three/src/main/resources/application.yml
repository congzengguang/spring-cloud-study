spring:
  application:
    name: eureka-ha

server:
  port: 8765


eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8762/eureka/
  instance:
    hostname: localhost
